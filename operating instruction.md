# 3D数学物理课件项目分工与协作说明文档

本说明文档明确了本项目中林海教授与我（负责全栈搭建与部署）的具体职责分工、内容接入规范以及线上版本迭代流程。

## 一、 职责分工

* **教授的职责（内容与核心逻辑）**
  * 提供数学物理课件的核心教学内容与大纲设计。
  * 编写或提供具体的 3D 渲染逻辑、数学公式演算代码以及各个章节的底层源码和原始素材。
  * 确保单个课件模块在本地运行时的数学准确性与逻辑正确性。

* **我的职责（全栈整合与运维部署）**
  * 负责项目的**全栈架构搭建**，管理整个 Git 仓库与目录层级规范。
  * 负责接收教授提供的源码，将其分类规整到对应目录中（如将子页面放入 `pages/`，大文件放入 `assets/`）。
  * 负责检查并修正所有静态资源的**相对路径**（确保页面、样式、脚本能正确加载）。
  * 负责修改根目录的 `index.html`，维护统一的全局导航入口。
  * 负责通过 GitHub Pages 平台进行项目的**在线发布、持续集成与线上访问维护**。

---

## 二、 协作与内容接入流程

当教授提供新的课件内容时，按照以下标准步骤进行整合上线：

* **文件归类**：将教授给的新课件文件（如 `ch3.html`）直接放入项目的 `pages/` 文件夹中。
* **路径对齐**：检查新文件的 `<head>` 标签，把样式表和脚本的引用路径调整为相对路径（例如将 `/css/style.css` 改为 `../css/style.css`）。
* **挂载入口**：打开根目录的 `index.html` 文件，在导航菜单处添加指向新课件的超链接代码：
  ```html
  <a href="pages/ch3.html" target="_blank">第三章：新课件标题</a>
同步上线：将修改完成的项目代码直接通过 GitHub 网页端（或 Git 命令行）上传至仓库根目录。提交（Commit）更改后等待 GitHub Pages 自动构建完成，即可实时更新线上链接。
